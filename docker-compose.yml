version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - redis
    volumes:
      - media_volume:/app/media

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  media_volume:
